# ===== Python =====
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ===== 运行时日志（不入库） =====
*.log
server.log

# ===== 手动备份 =====
data/*.backup.json

# ===== 截图 =====
site/screenshots/

# ===== 内部资料（不公开，避免推到公网仓库） =====
信息源网址清单.md

# ===== 运行时 / 增量状态 =====
# 注意：data/last_scrape.json 必须入库！GitHub Actions 每日定时抓取依赖它在
# 仓库中持久化，才能「接着上一次」做增量更新，而非每次全量重扫。
# （data/lectures.json 为公开讲座数据，本就入库。）
#
# 以下为本机 server.py 运行时生成的访问量/点赞统计状态，属本地运行时数据，
# 不应提交到仓库（公网为静态托管也不读取）。
data/visits.json
data/lecture_stats.json

# ===== 本地开发记忆与内部资料（不公开，避免推到公网仓库） =====
.workbuddy/
wechat_accounts.md

# ===== 本地专用后台页面：不部署到公网 =====
# 信息源管理依赖本地后端，公网不提供该功能。保留在本地开发用，
# 但绝不推送到仓库/Pages，避免暴露信息源结构与新增入口。
site/sources.html

# ===== 可能含凭据的本地环境文件（务必不要提交） =====
.env
.env.*
*.local
secrets.*
*token*.txt

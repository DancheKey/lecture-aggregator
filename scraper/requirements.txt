# 华师讲座采集器依赖（用于本地运行 / GitHub Actions CI）
# 2026-08-05 体检修复（中等-21）：固定版本，保证构建可复现——
# 上游一次破坏性/被污染的发布即可让每日 CI 变行为。升级请整体评估后同步改这里。
requests==2.32.4
beautifulsoup4==4.14.2
charset-normalizer==3.4.2
pyyaml==6.0.2
Pillow==12.0.0
numpy==2.3.4
# OCR 引擎：RapidOCR（ONNXRuntime 后端，替代原 easyocr；中文海报更准确、更轻量）
rapidocr-onnxruntime==1.4.4
opencv-python-headless==4.13.0.92

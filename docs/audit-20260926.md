# 全局一致性与安全审计报告（2026-09-26）

> 审计方式：四路并行（服务端安全 / 数据一致性 / 前端一致性 / 跨模块契约）+ 人工复核，
> 全部发现经逐条验证后按严重度分级。修复按 ①→⑤ 顺序执行于同日，提交
> e49b17e / e281a73 / 7ee2f87 / 08847aa / 73b5c39。
> 审计基线：3804 条记录（data/lectures.json @ 2026-09-26）、parsers.py 含候选瀑布 0-11。

## 总体结论

- **无 P0**：运行时字段契约（parsers → generate_frontend_data → server → 前端）逐键核对对齐，
  无 undefined 读取路径；无注入/路径遍历/危险反序列化；密钥零入库。
- **安全评级 B（良好）**：安全模型 = 「默认只绑 127.0.0.1」，本机使用风险低；
  对外部署前必须先解决写接口零鉴权（下表 S-1，仍未修）。
- 已修复：P1×4、P2×5（详见「修复跟踪」）；遗留低优先级项见「未修清单」。

## P1（当时已全部修复）

| # | 发现 | 位置 | 修复 |
|---|---|---|---|
| P1-1 | CI 只跑 5 个测试文件，候选9/10/11+质量闸门的全部行为锁定测试不在任何 workflow | .github/workflows/test.yml | ✅ e49b17e 补 6 项（含 schema 守卫） |
| P1-2 | 服务端写接口零鉴权，HOST=0.0.0.0 一键即暴露（sources 可注入采集源、/api/scrape 可反复触发） | server.py:265-339, 590-628, 690-694 | ✅ f66978c 随机 token 鉴权（发放端点仅回环） |
| P1-3 | docs/PARSING_RULES §1.6 停留在「MS1-MS5 模式A/B/C」时代；MS4 title 行描述与代码相反；「期次锚点」机制从未存在 | docs/PARSING_RULES.md | ✅ 73b5c39 重写 §1.5/§1.6/§1.8/§0 |
| P1-4 | docs §1.5 描述的 FC1-FC3/forumConference 机制从未实现（全仓 py 0 命中） | docs/PARSING_RULES.md | ✅ 73b5c39 勘误废弃；残留记录键已清理 |
| P1-5 | 职称/职务词表 6+ 处手抄副本与 field_vocab 主表分叉（「秘书长」一边放行一边判脏） | parsers.py / audit_data_quality.py | ✅ 08847aa 主表扩容16词、7处改派生 |

## P2（已修复 5 项）

| # | 发现 | 位置 | 修复 |
|---|---|---|---|
| P2-1 | 幽灵计数：拆分编号后 MS5 逐条剔除不重算——11页「标5/5实存1条」类失配（xz/230-252、ibc/2779、ai/163、swc/34、em/5295 等） | parsers.py MS5 过滤点 + 主库 | ✅ e281a73 解析器重算 + 存量规范化 + 2条单测 |
| P2-2 | 两个维护脚本全局 verify=False（MITM→主库投毒通道） | scripts/reparse_issues.py、fix_conf_splits.py | ✅ 7ee2f87 按域降级（仅 scnu.edu.cn 边界 SSLError 重试一次） |
| P2-3 | 错误响应泄露绝对路径/子进程 stderr/str(e) | server.py:562/611/625 | ✅ 7ee2f87 固定文案 + 详情落服务端日志 |
| P2-4 | 前端键引用无 schema 守卫（删字段前端静默读 undefined） | site/index.html + app.js | ✅ 73b5c39 新增 tests/test_frontend_schema.py 并入 CI |
| P2-5 | 「页脚/导航噪声」5 张同族词表并行维护、互有出入 | field_vocab/parsers | ⚠️ 部分未修（低风险，见未修清单） |

## P3（2026-09-26 二轮修复后状态）

1. **数据杂项**：✅ 15 条 lectureEnd 缺日期已补全、31 条 publishTime 已归一（f66978c 后续提交）；4 组 topic 重复已逐页核对——**em/6709 第2场 topic 错配按源页修正**，其余3组（em/8855 联合报告、cs/4145 论坛三人、xz/289 同活动多场次）属仓库既有惯例属正常；1 页 splitMode 组内混用（geography/45 的 vlm-poster+ocr-poster，属正常双路径）。
2. **遗留键**：旧 `affiliation`×24（现行代码只写 speakerAffiliation）、`id`×2、`topicPairedBy`×4（`__idx` 下发时已正确剥离）。
3. **未消费字段**：speakerTitle（3646条）/inviter（3148条）前端全站 0 读取——建议前端渲染（职称适合卡片展示）或注明有意保留。
4. **隐式跨模块契约**：「08:00/00:00=时刻未知占位」约定由解析器注释 + 前端 isTimeTBD 启发式两端各自实现，无共享常量；timeUnknown 字段唯一生产者是一次性脚本 clean_data_issues.py（前端有三态回落，行为自洽）。
5. **响应头**：✅ nosniff/X-Frame-Options/Referrer-Policy 已加、目录列表已禁（f66978c）；页面自身 CSP meta 维持不变。
6. **防刷**：✅ like 单条上限 999（f66978c）；✅ `GET /api/visits` 已补 Origin 校验；want 计数维持无上限（行为与 like 对称性弱、量级低，观察）。
7. **健壮性**：✅ POST body 上限 1MB + Content-Length 非数字守卫（f66978c）；附带修复 send_error 中文消息 latin-1 崩溃断连的既有潜在 bug。
8. **bat 脚本**：start_local.bat 强杀占用 8000 端口的任意进程；stop_local.bat 按「命令行含 server.py」模糊杀进程，可能误伤其他项目。
9. **parsers 内仍有少量字面职称交替式**（4406/4411/4684/4690/4700/5759/6172 等处，与 _SPEAKER_TITLE 近重复但语义微差）——本次收敛了审计点名的 7 处主副本，这些边角副本未动。
10. **test_frontend_consistency 的过滤 harness** 用精确匹配而非 .rstrip('/') 比较（与真实两端逻辑微差，当前数据无尾斜杠 URL，结果一致）。
11. **models prompt 第7条字段清单**仍是手写文本，未从 field_vocab 拼装（llm_provider.py:71-75）。

## 安全专项（评级 B）

**已验证无问题**：无 SQL/命令/模板注入（subprocess 全固定参数、yaml 全 safe_load、全仓 eval/exec/pickle 零命中）；无路径遍历（API 固定路径 + SimpleHTTPRequestHandler 自带 ..过滤，.tmp 已显式 404）；前端 XSS 面≈0（全 Vue 插值自动转义、无 v-html/innerHTML、safeUrl 白名单 http(s)）；密钥管理规范（.env 从未入库、CI 走 GitHub secrets、git 全历史无密钥——`sk-` 扫描命中均为英文摘要 "risk-free"）；依赖全部精确 pin 且为新版；点赞有 URL 白名单+IP 节流+原子写盘+防刷字典定期清理。

**对外部署前必须整改**：S-1 写接口鉴权（sources CRUD + /api/scrape 加随机 token 或强制回环）；P2-3 类信息泄露已修；建议顺带加 nosniff/CSP、关闭目录列表。

## 数据一致性验证方法备忘

- 主库 vs site：仅差派生键 `speakerKeys`（generate_frontend_data 加工，属设计行为，非复制失真）。
- 分片：chunks.json total=3804 与 8 片（7×500+304）一致；latest.json=按 lectureStart 前 50 一致。
- 拆分组：90 页全部 index 连续 1..M、count=M（修复后）；唯一键 (sourceUrl, lectureIndex) 无重复。
- college/campus 词表：campus 5 值规整；college 33 值无近重复/全半角问题（「人工智能学院」vs「阿伯丁数据科学与人工智能学院」为两所不同学院，非重复）。
